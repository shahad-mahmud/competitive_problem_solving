#include <iostream>
#include <map>
#include <vector>
using namespace std;

int main(){
    map<string, int> m;
    vector<string> mag, ran;
    int m, n;
    string s;

    cin >> m;
    cin >> n;

    for(int i = 0; i < m; i++){
        cin >> s;
        mag.push_back(s);
    }

    for(int i = 0; i < n; i++){
        cin >> s;
        ran.push_back(s);
    }

    for()

    return 0;
}